import os
# GitHub에 푸시하는 방법 (터미널에서)
# 1. GitHub에서 새 repo 생성: https://github.com/new -> kiwoom-factor-swing-bot
# 2. 로컬에서:
# git init
# git add .
# git commit -m "feat: factor swing + rescue bot"
# git branch -M main
# git remote add origin https://github.com/YOUR_USERNAME/kiwoom-factor-swing-bot.git
# git push -u origin main

# 또는 GitHub CLI로:
# gh repo create kiwoom-factor-swing-bot --public --source=. --remote=origin --push